new result, EasyEarnTask, taskMapCh in {
    contract EasyEarnTask(@"create", @nonce, @username, @hash, @deployerId, @key, ack) = {

    } |
    contract EasyEarnTask(@"hide", @nonce, @deployerId, @key, ack) = {
        
    } |
    contract EasyEarnTask(@"finish", @username, @nonce, @pk, @sig, ack) = {

    } |
    contract EasyEarnTask(@"find", @nonce, ack) = {

    } |
    contract EasyEarnTask(@"getNonce", ack) = {

    } |
    
    taskMapCh!({})
}